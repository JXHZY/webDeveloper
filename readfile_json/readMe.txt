Write a PHP script that uses information from spices.txt and creates
a map (associative array) with this data. Next, use loops to create a string in JSON format.
Call the above script using AJAX and display the content as is in the browser